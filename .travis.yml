sudo: required
services:
  - docker

before_install:
  - docker build -t bigz/docker-react -f Dockerfile.dev .

script:
  - docker run -e CI=true bigz/docker-react sh -c "npm run test -- --watchAll=false"